import React from 'react';

// import { generateMetaData } from '@/lib/utils';

import Notices from '@/app/(programs)/_components/notices';

// export const metadata = generateMetaData({});

const Page = () => {
  return <Notices department='physics' />;
};

export default Page;
