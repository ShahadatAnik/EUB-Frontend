import React from 'react';

// import { generateMetaData } from '@/lib/utils';

import AboutUs from '@/app/(programs)/_components/about-us';

// export const metadata = generateMetaData({});

const Page = () => {
  return <AboutUs department='bsc-cse' />;
};

export default Page;
